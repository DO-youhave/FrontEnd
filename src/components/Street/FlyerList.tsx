import styled from '@emotion/styled';

const FlyerList = () => {
  return (
    <Container>
      <li></li>
    </Container>
  );
};

export default FlyerList;

const Container = styled.ul`
  display: grid;
`;
